import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cn.learn48.voicelab',
  appName: '听读乐园',
  webDir: 'dist',
  android: { backgroundColor: '#F7F5EE' },
};
export default config;
